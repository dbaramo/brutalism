import styles from "./ImageSlider.module.css";

const imagesList = [
    
]

export default function ImageSlider(){
    return (
        <div className={styles.divStyle}>
            <p>image slider section here</p>
        </div>
        )
}